{
  "builds": [
    { "src": "api/**/*.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/webhook.py" }
  ],
  "crons": [
    {
      "path": "/api/send_attendance",
      "schedule": "50 16 * * *"
    }
  ]
}
